
  
  <!--  <% if current_user.favoriting?(microtask) %>-->
  <!--  <%= form_for(current_user.favorites.find_by(microtask_id: user.id), html: { method: :delete }) do |f| %>-->
  <!--    <%= hidden_field_tag :microtask_id, microtask.id %>-->
  <!--    <%= f.submit 'Unfavorite', class: 'btn btn-danger' %>-->
  <!--  <% end %>-->
  <!--<% else %>-->
  <!--  <%= form_for(current_user.favorites.build) do |f| %>-->
  <!--    <%= hidden_field_tag :microtask_id, microtask.id %>-->
  <!--    <%= f.submit 'favorite', class: 'btn btn-primary' %>-->
  <!--  <% end %>-->
  <!--<% end %>-->